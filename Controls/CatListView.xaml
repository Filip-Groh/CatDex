<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:CatDex.Models"
             xmlns:controls="clr-namespace:CatDex.Controls"
             x:Class="CatDex.Controls.CatListView"
             x:Name="this">

    <Grid RowDefinitions="Auto,Auto,*">
        <ActivityIndicator IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Grid.Row="0"
                           Margin="0,10,0,10"/>

        <Grid Grid.Row="1"
              Margin="8,0,8,8"
              ColumnDefinitions="*,Auto">
            <Picker ItemsSource="{Binding Breeds}"
                    SelectedItem="{Binding SelectedBreed}"
                    ItemDisplayBinding="{Binding Name}"
                    Title="Filter by Breed"
                    Grid.Column="0"
                    VerticalOptions="Center"/>

            <Border Grid.Column="1"
                    StrokeThickness="0"
                    Padding="2"
                    HeightRequest="40"
                    VerticalOptions="Center"
                    BackgroundColor="{AppThemeBinding Light=White, Dark=Transparent}"
                    StrokeShape="RoundRectangle 6">
                <HorizontalStackLayout Spacing="2">
                    <Button Command="{Binding ToggleDisplayModeCommand}"
                            ImageSource="grid.svg" Style="{StaticResource ToggleIconButton}">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button"
                                         Binding="{Binding IsListMode}"
                                         Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Button"
                                         Binding="{Binding IsListMode}"
                                         Value="True">
                                <Setter Property="Opacity" Value="0.3"/>
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                    <Button Command="{Binding ToggleDisplayModeCommand}"
                            ImageSource="list.svg" Style="{StaticResource ToggleIconButton}">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button"
                                         Binding="{Binding IsListMode}"
                                         Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Button"
                                         Binding="{Binding IsListMode}"
                                         Value="False">
                                <Setter Property="Opacity" Value="0.3"/>
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                </HorizontalStackLayout>
            </Border>
        </Grid>

        <RefreshView IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding OnRefreshCommand}"
                     Grid.Row="2"
                     Margin="8,0">
            <CollectionView ItemsSource="{Binding Cats}"
                            SelectionMode="None">

                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" 
                                     Span="2" 
                                     HorizontalItemSpacing="8"
                                     VerticalItemSpacing="8"/>
                </CollectionView.ItemsLayout>

                <CollectionView.Triggers>
                    <DataTrigger TargetType="CollectionView"
                                 Binding="{Binding IsListMode}"
                                 Value="True">
                        <Setter Property="ItemsLayout">
                            <Setter.Value>
                                <LinearItemsLayout Orientation="Vertical" 
                                                   ItemSpacing="8"/>
                            </Setter.Value>
                        </Setter>
                    </DataTrigger>
                </CollectionView.Triggers>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Cat">
                        <controls:CatCard ToggleFavoriteCommand="{Binding Source={x:Reference this}, Path=BindingContext.ToggleFavoriteCommand}"
                                          TappedCommand="{Binding Source={x:Reference this}, Path=BindingContext.NavigateToCatDetailsCommand}"
                                          DeleteCommand="{Binding Source={x:Reference this}, Path=BindingContext.DeleteCatCommand}"
                                          IsListMode="{Binding Source={x:Reference this}, Path=BindingContext.IsListMode}"/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <ContentView>
                        <VerticalStackLayout HorizontalOptions="Center"
                                             VerticalOptions="Center"
                                             Padding="20">
                            <Label Text="{Binding Source={x:Reference this}, Path=EmptyViewText}"
                                   FontSize="16"
                                   HorizontalOptions="Center"
                                   TextColor="Gray"/>
                        </VerticalStackLayout>
                    </ContentView>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentView>
