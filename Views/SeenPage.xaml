<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:CatDex.Models"
             xmlns:viewmodels="clr-namespace:CatDex.ViewModels"
             xmlns:controls="clr-namespace:CatDex.Controls"
             x:Class="CatDex.Views.SeenPage"
             x:DataType="viewmodels:SeenViewModel"
             Title="SeenPage">

    <Grid RowDefinitions="Auto,Auto,*">
        <ActivityIndicator IsRunning="{Binding IsBusy}"
                           IsVisible="{Binding IsBusy}"
                           Grid.Row="0"
                           Margin="0,10,0,10"/>

        <Button Text="{Binding IsListMode, Converter={StaticResource InvertedBoolConverter}, ConverterParameter='Gallery|List'}"
                Command="{Binding ToggleDisplayModeCommand}"
                HorizontalOptions="End"
                Margin="8,0"
                Grid.Row="1">
            <Button.Triggers>
                <DataTrigger TargetType="Button"
                             Binding="{Binding IsListMode}"
                             Value="True">
                    <Setter Property="Text" Value="Gallery"/>
                </DataTrigger>
                <DataTrigger TargetType="Button"
                             Binding="{Binding IsListMode}"
                             Value="False">
                    <Setter Property="Text" Value="List"/>
                </DataTrigger>
            </Button.Triggers>
        </Button>

        <RefreshView IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}"
                     Grid.Row="2">
            <CollectionView ItemsSource="{Binding Cats}"
                            SelectionMode="None">

                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical" 
                                     Span="2" 
                                     HorizontalItemSpacing="8"
                                     VerticalItemSpacing="8"
                                     x:Name="gridLayout"/>
                </CollectionView.ItemsLayout>

                <CollectionView.Triggers>
                    <DataTrigger TargetType="CollectionView"
                                 Binding="{Binding IsListMode}"
                                 Value="True">
                        <Setter Property="ItemsLayout">
                            <Setter.Value>
                                <LinearItemsLayout Orientation="Vertical" 
                                                   ItemSpacing="8"/>
                            </Setter.Value>
                        </Setter>
                    </DataTrigger>
                </CollectionView.Triggers>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Cat">
                        <controls:CatCard ToggleFavoriteCommand="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SeenViewModel}}, Path=ToggleFavoriteCommand}"
                                          TappedCommand="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SeenViewModel}}, Path=NavigateToCatDetailsCommand}"
                                          DeleteCommand="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SeenViewModel}}, Path=DeleteCatCommand}"
                                          IsListMode="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:SeenViewModel}}, Path=IsListMode}"/>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <ContentView>
                        <VerticalStackLayout HorizontalOptions="Center"
                                             VerticalOptions="Center"
                                             Padding="20">
                            <Label Text="No cats found in database"
                                   FontSize="16"
                                   HorizontalOptions="Center"
                                   TextColor="Gray"/>
                        </VerticalStackLayout>
                    </ContentView>
                </CollectionView.EmptyView>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>
